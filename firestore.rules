
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // PUBLIC TEST MODE: Allows unrestricted access to all documents.
    // Ensure you create the required composite indexes in the Firebase Console.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
